name: Check
on:
  push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: CheckOut
        uses: actions/checkout@v3

      - name: Install deps
        run: npm install

      - name: Lint
        run: npm run lint
